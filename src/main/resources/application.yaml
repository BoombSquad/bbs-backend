spring:
   application:
    name: 'lab-eng-software'

   datasource: 
    url: jdbc:postgresql://localhost:5432/BombSquadDB
    username: postgres
    password: 123

spring.jpa:
  hibernate:
    ddl-auto: none
  show-sql: false
  properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQL81Dialect